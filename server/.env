# Development .env for backend (auto-created). Replace values before production.
MONGODB_URI=mongodb://127.0.0.1:27017/portfolio
JWT_SECRET=change_this_secret_to_a_long_random_value
ADMIN_PASSWORD=change_admin_password
PORT=5000
